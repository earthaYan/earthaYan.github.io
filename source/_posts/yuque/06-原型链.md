
---

title: 06-原型链

urlname: sott73

date: 2019-12-19 17:51:43 +0800

tags: 
  - js
  - 原型链
categories: 前端
---
<a name="j0WwH"></a>

### 问题描述：
1. 创建对象有几种方法
1. 原型，构造函数，对象实例，原型链
1. instanceof原理
1. new运算符，工作原理

<a name="DSDUb"></a>

### 创建对象的方法:

```javascript
var obj={name:'andy'}	//字面量创建对象，默认原型指向Object,所以两个可以划分为一类
var obj=new Object({name:'andy'})
//Object({name:'andy'})
```

```javascript
//使用显式的构造函数创建对象
var OBJ=function(){
  this.name="andy"
}
var o=new OBJ()
//OBJ
```

```javascript
var P={name:'andt'}
var obj=Object.create(P)
OBJECT{}
```

<a name="l5m63"></a>

### 原型，构造函数，实例，原型链：
![image.png](https://cdn.nlark.com/yuque/0/2019/png/115484/1576807438946-76dd4584-64e1-466b-90e6-026c97114350.png#align=left&display=inline&height=312&name=image.png&originHeight=312&originWidth=836&size=61491&status=done&style=none&width=836)

1. 实例：obj1,obj2,obj3
1. 构造函数：new 后面的函数，即使没有this
1. 原型链：通过prototye(构造**函数**)和_proto_（原型对象）层层向上寻找prototype,顶端是Object.prototype终止
1. OBJ._proto_==Function.prototype

<a name="Ihg65"></a>

### instanceof:
实例对象._proto` instanceof ` 构造函数.prototype<br />![image.png](https://cdn.nlark.com/yuque/0/2019/png/115484/1577064930089-05f3acac-e085-4b34-9b4b-24c9dbb6a712.png#align=left&display=inline&height=189&name=image.png&originHeight=189&originWidth=819&size=42857&status=done&style=none&width=819)

<a name="FOPUM"></a>

### new 运算符：

1. 一个新的空对象被创建。继承自构造函数的原型对象

```javascript
var foo=function(){this.name="ang"}
var f=new foo()//创建空对象f,继承自foo.prototype
```

2. 构造函数foo被执行。执行的时候，相应的传参会被传入,同时上下文（this）会被指定为这个新实例
2. 如果构造函数返回了一个"对象",那么这个对象会取代整个new出来的结果，如果构造函数没有返回对象，则new出来的结果为步骤11创建的对象<br />

```javascript
//模拟new过程
var new2=function(func){
  //创建空对象，空对象关联构造函数的原型对象
  var o=Object.create(func.prototype)
  //执行构造函数
  var k=func.call(o)
  //判断构造函数的运行结果是不是对象类型
  if(typeof k == 'object'){
    return k
  }
  else{
    return o
  }
}
```


